import React from 'react';
import CrossPlatformIcon from 'react-native-cross-platform-icons';

export default function GoogleIcon({...rest}) {
  return (
    <CrossPlatformIcon name="logo-google" size={30} color="white" outline />
  );
}
